const localhost = 'http://127.0.0.1:5000';

const apiURL = '/api';

export const endpoint = `${localhost}${apiURL}`;

export const ckdHybridURL = `${endpoint}/predict-ckd-hybrid/`;
export const ckdGFRURL = `${endpoint}/predict-ckd-gfr/`;